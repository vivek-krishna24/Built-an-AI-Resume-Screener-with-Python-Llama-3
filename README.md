# Built-an-AI-Resume-Screener-with-Python-Llama-3
Resume Screening AI Agent (Local LLM)

A local AI-powered resume screening tool that analyzes PDF resumes against a job description using Ollama + LLaMA3.
The system extracts resume text, sends it to a local LLM, and returns a structured JSON-based hiring recommendation.

🚀 Features

📄 Extracts text from PDF resumes

🤖 Uses local LLM (LLaMA3 via Ollama) — no cloud dependency

📊 Generates a match score (0–100)

✅ Identifies key strengths

❌ Detects missing critical skills

🧾 Outputs strict, machine-readable JSON

🔒 Safe JSON parsing with auto-fix for common LLM issues
